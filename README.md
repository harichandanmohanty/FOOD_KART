# FOOD_KART
A food ordering app with static data and static html

To run this project -

1. Clone repo
2. check if node and npm  installed , check node -v, npm -v
3. npm install
4. npx parcel index.html